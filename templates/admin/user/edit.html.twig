{# filepath: templates/admin/users/edit.html.twig #}
{% extends 'base.html.twig' %}
{% block title %}Modifier un utilisateur{% endblock %}
{% block body %}
<div class="container my-5 p-5 col-lg-5">
    {{ affichageDesMessages }}
    <h3>Modifier {{ user.login }}</h3>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
</div>
{% endblock %}
